# Only needed in production, or if you don't want to use the Vite dev proxy.
# Point this at your deployed server's base URL, e.g.:
# VITE_API_BASE=https://taskflow-pro-server.onrender.com
VITE_API_BASE=/api
